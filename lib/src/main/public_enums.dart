enum UserDatabaseServiceMode { Debug, Firestore }

enum MessageDatabaseServiceMode { Debug, Firestore }

enum NotificationServiceMode { Debug, FirebaseCloudMessaging }

enum NotificationLocalServiceMode { Debug, OnlinePackage }

enum StorageServiceMode { Debug, FirebaseStorage }

enum MessageDatabaseResult { Success, Error }

enum NotificationResult { Success, Error }
